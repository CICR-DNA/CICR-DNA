package fr.insalyon.mxyns.icrc.dna.sync;

/**
 * Synchronizer that uses emails to send the cases data
 */
public class EmailSync implements Sync {

    @Override
    public boolean send(String filePath) {

        return true;
    }
}
